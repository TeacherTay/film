/* Reset e fundo de tela cheia */
html, body {
  margin: 0;
  padding: 0;
  width: 100%;
  height: 100%;
  overflow: hidden;
  font-family: 'Segoe UI', sans-serif;
  background: linear-gradient(135deg, #8a2be2, #da70d6, #ffb6c1);
  background-size: 400% 400%;
  animation: gradient 15s ease infinite;
}

@keyframes gradient {
  0% {background-position: 0% 50%;}
  50% {background-position: 100% 50%;}
  100% {background-position: 0% 50%;}
}

#interface-p5 {
  width: 100vw;
  height: 100vh;
  position: relative;
}

/* Estilos para elementos criados pelo p5.js */
h2, span, input, select, a, button {
  color: white;
  font-family: 'Georgia', serif;
}

input, select {
  padding: 6px;
  margin: 5px;
  border-radius: 6px;
  border: none;
}

button {
  background-color: white;
  color: #8a2be2;
  font-weight: bold;
  border: none;
  padding: 10px 20px;
  border-radius: 8px;
  cursor: pointer;
}

button:hover {
  background-color: #f0f0f0;
}

a {
  display: inline-block;
  margin-top: 10px;
  background: white;
  padding: 8px 14px;
  border-radius: 8px;
  color: #8a2be2;
  text-decoration: none;
  font-weight: bold;
}

a:hover {
  background: #f0f0f0;
}
